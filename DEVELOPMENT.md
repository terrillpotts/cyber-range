# Development notes

Maintainer notes for editing this project. See `README.md` for the public-facing project overview.

**Two published copies exist and both need updating on changes:**
- Claude Artifact: https://claude.ai/code/artifact/3831172f-1950-4713-80f9-6eb58ad0c3d0 (republish via the Artifact tool, passing that `url` so it updates in place instead of minting a new one)
- GitHub Pages: https://terrillpotts.github.io/proving-grounds/ (served from `index.html` on the `main` branch of the `terrillpotts/proving-grounds` repo — commit/push to update)

**Source of truth for editing:** `proving-grounds.html` in this folder is the durable copy (any Claude session's scratchpad copy is temporary and disappears when that session ends). The version pushed to GitHub as `index.html` should always match it. (Renamed from `cysa-range.html` on 2026-08-18 when the platform was rebranded from "The Cyber Range" to "The Proving Grounds" — the repo was renamed from `cyber-range` to `proving-grounds` at the same time.)

### Objectives alignment (added 2026-08-13)

Terrill supplied the official CompTIA CySA+ CS0-003 Exam Objectives PDF (Document Version 6.0) and asked that all content be rebased against it. A full gap analysis against every sub-bullet in objectives 1.1–4.2 turned up several sub-areas the original build never covered — most notably **2.4 (specific vulnerability/attack types: XSS, injection, SSRF, RCE, overflows, etc.) was entirely missing** despite being a real, testable chunk of the 30%-weight D2 domain. That gap plus named-tool recognition (Nessus, Burp Suite, Nmap, WHOIS, VirusTotal, etc.), OS/network architecture fundamentals (Registry, SASE, SDN, IAM types, PKI, DLP/PII/CHD), and the 2.5 control-type/attack-surface/secure-coding/SDLC taxonomy were addressed with new lessons+quizzes(+labs where hands-on practice fit). Smaller gaps (OSSTMM/OWASP Testing Guide, re-imaging, BC/DR, inhibitors to remediation, incident declaration/escalation, alert volume/SLOs/Top 10) were folded into existing lessons as targeted additions rather than new nodes. **If asked to extend this further, re-check the objectives PDF against `QUIZZES`/`LESSONS` keys first — don't assume the last audit is still complete after adding new content.**

## Architecture (all in one `<script>` tag, IIFE)

- `QUIZZES` — all quiz question banks, keyed by lesson, shared across all sections.
- `LESSONS` — lesson HTML content, keyed by lesson, shared across all sections.
- `LABS` — terminal lab file systems + objectives, keyed by lab, shared across all sections.
- `DOMAINS_CYBERSECURITY` / `DOMAINS_AZURE` / `DOMAINS_AWS` — each leaf's domains, an ordered array with `nodes` (lesson/lab refs, XP, unlock sequencing) per domain. Same shape across all three.
- `SECTIONS` — a **tree**, not a flat list: `cybersecurity` and `networking` are top-level leaves; `cloud` is a **group** (`kind:'group'`) with no domains/state of its own, holding `subsections: [azure, aws]` — each subsection is a leaf in the exact same shape as a top-level leaf (`domains`, `ranks`, `examPool`, `examComposition`, `examPassPct`, `examXP`, `examTitle`, `examBlueprint`, `examCertifiedLabel`). See "Sections architecture" below for the full group/leaf nesting model — added 2026-08-19 when Cloud was split into Azure and AWS.
- `LEAF_SECTIONS` — a flat array of every leaf (a group's subsections hoisted up alongside the top-level leaves), built once from `SECTIONS` at load. `sectionById(id)` searches this, not `SECTIONS` — a group id like `'cloud'` is never a valid leaf id and won't resolve here; use `groupById(id)` for that.
- `groupById(id)` / `parentGroupOf(leafId)` — `parentGroupOf` returns the group a leaf is nested under (e.g. `parentGroupOf('azure')` → the `cloud` group object), or `null` for a top-level leaf like `cybersecurity`. Powers the "Cloud /" breadcrumb prefix and the nav's group-expansion logic.
- `EXAM_POOL_CYBERSECURITY` / `EXAM_POOL_AZURE` / `EXAM_POOL_AWS` (+ `_COMPOSITION`, `_PASS_PCT`, `_XP`, `_TITLE`, `_BLUEPRINT`, `_CERTIFIED_LABEL` per leaf) / `buildExam()` — final exam, built from `QUIZZES` (no duplicated content). `buildExam()` itself is leaf-agnostic — it just reads whichever bare `EXAM_POOL`/`EXAM_COMPOSITION` are currently swapped in.
- `numberWord(n)` — spells out small integers (`3` → `"three"`) so dashboard/exam copy like "across three domains" stays grammatical regardless of how many domains a leaf has, instead of hardcoding a word.
- `shuffleQuestion()` — reorders each question's options at render time (see "Known gotchas" below — this was a real bug, don't remove it).
- **Reference-swap pattern:** `state`, `DOMAINS`, `RANKS`, `EXAM_POOL`, `EXAM_COMPOSITION`, `EXAM_PASS_PCT`, `EXAM_XP`, `EXAM_TITLE`, `EXAM_BLUEPRINT`, `EXAM_CERTIFIED_LABEL` are plain top-level `var`s that get *reassigned* by `setSection(id)` to point at whichever **leaf** is currently active (`state = sectionStates[id]`, `DOMAINS = sec.domains`, `RANKS = sec.ranks`, `EXAM_POOL = sec.examPool`, etc.) — `setSection()` only ever takes a leaf id (`'azure'`, `'aws'`, `'cybersecurity'`, `'networking'`), never a group id like `'cloud'`, since a group has nothing to swap in. Every pre-existing function (`domainById`, `overallProgress`, `mountQuiz`, `renderLab`, `mountExam`, `getRank`, etc.) still reads the bare globals unmodified; they transparently operate on whatever leaf is active because the variable itself was swapped, not because those functions know about sections. **Don't "fix" this by threading a `sectionId` param through those functions — that's the whole point of the pattern.** The deliberate exceptions are `renderHome()`/`renderGroupPage()` (via the shared `renderSectionCard()` helper), which show *multiple* sections/leaves at once regardless of which is "current" — they call `getRank(st.xp, item.ranks)` and `sectionOverallProgress(item)` to read each item's own state/ranks directly, bypassing the swapped globals.
- `sectionStates` / `STORAGE_KEY` (`range_cysa_progress_v1`) — one `{xp, completed, quizScores, labProgress, streak, lastVisit, examPassed, examAttempts, examBest}` object per **leaf** (keyed by leaf id, e.g. `azure`/`aws`/`cybersecurity`/`networking` — never `cloud`), persisted together under a single `localStorage` key. `normalizeSectionStates()` iterates `LEAF_SECTIONS` (not `SECTIONS`) and migrates the old pre-sections flat-object shape into `sections.cybersecurity` on load, so existing users don't lose progress. Export/Import now serialize/restore all leaves at once, not just the active one.
- `render()` / `view` — hand-rolled router. `view.type` is `home` / `group` / `dashboard` / `domain` / `lesson` / `lab` / `exam`. `group` carries `groupId` (e.g. `{type:'group', groupId:'cloud'}`, the Cloud landing page listing Azure/AWS); every other non-home type carries `sectionId` (always a leaf id). `go(v)` calls `setSection(v.sectionId)` before switching `view`, whenever `v.sectionId` is present — a `group` view has none, so it deliberately leaves `state`/`DOMAINS`/etc pointing at whatever leaf was last active, since the group page doesn't need them (it reads `sectionStates` directly per-subsection instead).

### Sections architecture (added 2026-08-18, extended to groups 2026-08-19)

Three top-level tracks sit above domains: **Cybersecurity** (CS0-003) is a plain leaf; **Cloud** is a **group** holding **Azure** (AZ-900) and **AWS** (CLF-C02) as independently-tracked leaf subsections; **Networking** is an empty placeholder leaf — a real nav destination that renders a "coming soon" dashboard via the `if(!DOMAINS.length)` early-return in `renderDashboard()`. XP/rank/streak/exam progress are tracked independently per **leaf** via `sectionStates` (never per-group — a group has no progress of its own, only an aggregate rollup computed on the fly for its card). The Home page (`renderHome()`) lists all 3 top-level `SECTIONS` entries; a group entry renders as an aggregate card (total XP and modules-complete summed across its subsections) that links to a new `renderGroupPage()` — a mini "Home" scoped to just that group's subsections, with its own back-link. Both pages share one `renderSectionCard(item)` helper that branches on `item.kind` (`'leaf'` vs `'group'`) so a group card never assumes `item.domains`/`sectionStates[item.id]` exist (a group has neither).

**Nav sidebar nesting:** the sidebar now supports two indent levels — `.nav-sub` (14px, a top-level leaf's domains, or a group's subsections) and `.nav-sub2` (26px, domains under a subsection nested inside a group), plus a matching `.nav-mini-bar.sub2` for the progress bar indent. `renderChrome()`'s nav-building loop branches per top-level `SECTIONS` entry: a `kind:'group'` entry expands to list its subsections (each showing its own rollup %) when the group itself is "active" (either you're on the group's own landing page, or the currently active leaf is one of its subsections — via `parentGroupOf`), and a subsection further expands to its own domain list (at the deeper `nav-sub2` indent) only when *that specific subsection* is the active leaf. A `isLeafView` boolean (true only for `dashboard`/`domain`/`lesson`/`lab`/`exam` view types) gates all of this expansion logic — **this exists to fix a real bug**: without it, `currentSectionId` (which only updates via `setSection()`, i.e. only on leaf-carrying views) stays stale on `home` and `group` views, and an unrelated top-level leaf (whatever was last visited) would incorrectly render as "expanded" on the Home or Cloud landing page. Breadcrumbs get the same treation via `parentGroupOf(currentSectionId)` — a leaf nested in a group gets a `"Cloud / "` prefix before its own name (e.g. `Home / Cloud / AWS / Final Exam`); a top-level leaf like Cybersecurity does not.

To add real content to Networking later (as a 4th top-level **leaf**, not nested in a group): build a `DOMAINS_NETWORKING` array in the exact shape of the existing `DOMAINS_*` arrays, plus its own `RANKS_NETWORKING` ladder and `EXAM_POOL_NETWORKING`/`EXAM_COMPOSITION_NETWORKING`/`EXAM_PASS_PCT_NETWORKING`/`EXAM_XP_NETWORKING`/`EXAM_TITLE_NETWORKING`/`EXAM_BLUEPRINT_NETWORKING`/`EXAM_CERTIFIED_LABEL_NETWORKING`, then point that section's fields at them in `SECTIONS` (`kind:'leaf'`, same as Cybersecurity). To add a **new platform nested inside an existing group** (e.g. a third cloud provider under Cloud), or a **brand-new group** (a new top-level category with its own subsections): follow the exact same per-leaf pattern, but add the leaf object into a group's `subsections` array (or create a new `{ id, kind:'group', name, tagline, status, subsections:[...] }` entry in `SECTIONS`) instead of adding it directly to `SECTIONS`. Either way, nothing else about nav, dashboard, domain/lesson/lab/exam rendering, XP awarding, or progress tracking needs to change — that's the payoff of the reference-swap pattern above, now proven across Cybersecurity (original), Azure (first 2nd-section test), and AWS (first group-nesting test).

### Cloud (AZ-900) track (added 2026-08-19)

Terrill asked for a full track under Cloud, based on the official Microsoft Learn AZ-900 study guide PDF (`Study guide for Exam AZ-900_ Microsoft Azure Fundamentals _ Microsoft Learn.pdf`), built the same way as the Cybersecurity/CS0-003 content — same lesson/quiz/lab/capstone shape, same XP/lock/unlock mechanics, same terminal engine (reused as-is for scenario-matching labs rather than literal CLI commands, since AZ-900 doesn't test hands-on CLI). This was also the first real population of the `cloud` section (before it was later split into a group — see below), which required generalizing several things that were previously Cybersecurity-only hardcodes:

- **Domains**: `DOMAINS_AZURE` (originally `DOMAINS_CLOUD`, renamed when Cloud became a group — see below) — D1 Cloud Concepts (28%, 5 nodes/950 XP), D2 Azure Architecture & Services (38%, 7 nodes/1400 XP), D3 Azure Management & Governance (34%, 6 nodes/1100 XP). Weights roughly match the real AZ-900 blueprint (25-30/35-40/30-35%).
- **Ranks**: `RANKS_AZURE` — Azure Trainee through Azure MVP, scaled to the smaller ~3,450 XP domain total (vs. Cybersecurity's ~6,750) rather than reusing `RANKS_CYBERSECURITY`'s SOC-themed names/thresholds.
- **Exam**: `EXAM_POOL_AZURE`/`EXAM_COMPOSITION_AZURE` — 20 questions, 6/8/6 split by domain. `EXAM_PASS_PCT_AZURE = 70` (matches the real AZ-900 passing score of 700/1000) vs. Cybersecurity's 80%. `EXAM_XP_AZURE = 600`. Title "AZ-900 Final Exam", certified badge "AZ-900 Certified".
- **Generalized UI text**: several strings were hardcoded to "CS0-003"/"CySA+ Certified"/"four domains"/"20 questions" across `renderChrome`, `renderDashboard`, `renderDomain`, `renderExam`, `mountExam` — these now read `EXAM_BLUEPRINT`/`EXAM_TITLE`/`EXAM_CERTIFIED_LABEL`/`numberWord(DOMAINS.length)`/the summed `EXAM_COMPOSITION` count instead, so the exact same render code produces correct copy for any leaf.
- **Real bug found and fixed during this build**: `LABS.*.title` fields are used in two places — raw HTML insertion in `renderLab()` (wants `&amp;` for a literal `&`) and `showToast('LAB COMPLETE', lab.title)` in `maybeCompleteLab()`, which runs the title through `escapeHtml()` (wants a plain `&`, since escaping already handles the entity). Every pre-existing Cybersecurity lab title happened to have no ampersand, so this dual-usage conflict was latent and untested. The new lab titles that did contain `&` were authored with `&amp;` (copying the `LESSONS` title convention, which is *only* ever raw-inserted and has no such conflict) and surfaced as a literal `&amp;` in the "LAB COMPLETE" toast. Fixed by using a plain `&` in `LABS.*.title` specifically — browsers tolerate a bare `&` in raw HTML text content fine, so this satisfies both call sites. **If a new lab title needs an ampersand, use a plain `&`, not `&amp;`.** (This bit again during the AWS build below for the same reason — same fix, applied up front that time.)
- Confirmed via live click-through testing (completed all of D1 through the terminal including a multi-objective capstone, spot-checked D2/D3 lesson rendering, shortcut-completed the remaining nodes via direct `localStorage` write to reach 100%, then took and passed the real 20-question final exam end-to-end) — no console errors on either published copy afterward.

### Cloud split into Azure + AWS group (added 2026-08-19, same day, separate follow-up request)

Terrill asked to make Cloud hold two independently-tracked platforms — Azure and AWS — rather than Cloud itself being a single AZ-900-only track. Confirmed via AskUserQuestion: **two independent sub-tracks** (own domains/XP/rank/exam each, Cloud becomes a landing page one level above them) over a single shared-XP-pool alternative, and **AWS Cloud Practitioner (CLF-C02)** over Solutions Architect Associate for the AWS content (parallel fundamentals-level pairing with AZ-900). This is the change described in detail in "Sections architecture" and the "Architecture" bullets above (the group/leaf tree, `LEAF_SECTIONS`, `groupById`/`parentGroupOf`, `isLeafView`, nav-sub2) — this entry is the "what and why," that one is the "how."

- **Renamed** `DOMAINS_CLOUD`→`DOMAINS_AZURE`, `RANKS_CLOUD`→`RANKS_AZURE` (+ renamed rank names `Cloud Trainee`→`Azure Trainee`, `Cloud Solutions Associate`→`Azure Solutions Associate`, `Cloud Architect`→`Azure Architect`, `Senior Cloud Architect`→`Senior Azure Architect`, `Principal Cloud Architect`→`Principal Azure Architect`), and every `EXAM_*_CLOUD`→`EXAM_*_AZURE` identifier — purely a rename, zero behavior change, since "Cloud" no longer uniquely means Azure once AWS exists alongside it.
- **New AWS content**: `DOMAINS_AWS` — D1 Cloud Concepts (24%, 4 nodes/850 XP), D2 Security & Compliance (30%, 5 nodes/1000 XP), D3 Cloud Technology & Services (34%, 7 nodes/1400 XP), D4 Billing, Pricing & Support (12%, 3 nodes/550 XP) — weights match the real CLF-C02 blueprint exactly. `RANKS_AWS` (AWS Trainee → AWS Hero, same XP-threshold shape as Azure's ladder). `EXAM_POOL_AWS`/`EXAM_COMPOSITION_AWS` — 20 questions, 5/6/7/2 split by domain, `EXAM_PASS_PCT_AWS = 70` (real CLF-C02 passing score), `EXAM_XP_AWS = 550`, title "CLF-C02 Final Exam", certified badge "AWS Cloud Practitioner Certified". 11 lessons / 4 labs / 4 capstones / 66 quiz questions, same content depth as the Azure track.
- **`SECTIONS` restructured from a flat array into a tree**: `cloud` changed from a leaf with its own `domains` into `{ id:'cloud', kind:'group', subsections:[azureLeaf, awsLeaf] }`. Added `LEAF_SECTIONS` (flat list for state/lookup), `groupById()`, `parentGroupOf()`. `sectionById()` now searches `LEAF_SECTIONS` instead of `SECTIONS`.
- **New `view.type === 'group'`**: `renderGroupPage(groupId)` (Cloud's own landing page, listing Azure/AWS) alongside the existing `renderHome()`, sharing a new `renderSectionCard(item)` helper (branches on `item.kind`) and `wireSectionCards()` click-binding helper. `render()`'s switch got one new branch for it.
- **Real bug found and fixed during this build**: the nav's "is this top-level leaf currently expanded" check was `view.type!=='home' && currentSectionId===sec.id` — true for the Home page's absence of a real "current" leaf was fine before groups existed (view.type was always home/dashboard/domain/lesson/lab/exam, and dashboard+ always set `currentSectionId` correctly via `setSection`), but the new `group` view type doesn't call `setSection()` (a group has no state to switch to), so `currentSectionId` stays stale at whatever leaf was last active — e.g. landing on the Cloud group page while `cybersecurity` was the last-active leaf incorrectly rendered Cybersecurity's domain list as expanded in the sidebar, on top of Cloud's own expansion. Fixed by adding the `isLeafView` boolean (true only for `dashboard`/`domain`/`lesson`/`lab`/`exam`) and gating all three "is this expanded" checks (`isActiveSection`, `activeLeafInGroup`, `subIsActive`) on it instead of just `view.type!=='home'`.
- Confirmed via live click-through testing: Home shows Cybersecurity/Cloud(group, aggregate "2 tracks · N XP")/Networking; Cloud landing page shows Azure/AWS cards with independent XP/rank/progress; nav correctly nests 3 levels deep (Cloud → AWS → D1 domains) with no stray-expansion bug after the fix; completed an AWS lesson quiz and a multi-objective lab live through the terminal (confirmed the ampersand fix held); shortcut-completed the rest of AWS via direct `localStorage` write and took/passed the real 20-question CLF-C02 exam end-to-end (correct 5/6/7/2 domain breakdown, XP award, "AWS Cloud Practitioner Certified" badge); re-verified Cybersecurity and Azure both still render and behave identically post-refactor. No console errors on either published copy.

## How to keep developing this

1. Edit `proving-grounds.html` directly (plain HTML/CSS/JS, no build step).
2. Test locally before publishing — **do not trust bracket-balance checks alone**, click through it in a real browser. A local static server works well:
   ```powershell
   # from this folder
   $listener = New-Object System.Net.HttpListener
   $listener.Prefixes.Add("http://localhost:8791/")
   $listener.Start()
   while ($true) {
     $context = $listener.GetContext()
     $path = $context.Request.Url.LocalPath.TrimStart('/')
     if ([string]::IsNullOrEmpty($path)) { $path = "proving-grounds.html" }
     $full = Join-Path (Get-Location) $path
     if (Test-Path $full) {
       $bytes = [System.IO.File]::ReadAllBytes($full)
       $context.Response.ContentType = "text/html; charset=utf-8"
       $context.Response.OutputStream.Write($bytes,0,$bytes.Length)
     } else { $context.Response.StatusCode = 404 }
     $context.Response.OutputStream.Close()
   }
   ```
   Then open `http://localhost:8791/proving-grounds.html`. (`file://` also works in a normal browser, just not inside Claude Code's browser-automation tool, which blocks local file navigation.)
3. Republish to the *same* Claude artifact URL by passing `url: "https://claude.ai/code/artifact/3831172f-1950-4713-80f9-6eb58ad0c3d0"` to the Artifact tool — otherwise a new conversation mints a brand-new URL.
4. Push the same updated file to GitHub as `index.html` on `main` (repo: `terrillpotts/proving-grounds`) so the GitHub Pages copy matches.
5. After any content edit, re-copy this file back into the durable project folder (`C:\projects\cyber projects\proving-grounds\proving-grounds.html`) — the scratchpad copy a session works from is temporary.

## Known gotchas (found via testing, already fixed — don't reintroduce)

- **Answer-position bias:** when authoring quiz questions, the correct option kept landing in the same slot (a real bug found on 60/64 questions — all `correct:1`). Fixed via `shuffleQuestion()`, applied in both `mountQuiz()` and `buildExam()`. If you add new questions, you don't need to manually vary option order — the shuffle handles it — but don't remove the shuffle call.
- **Sidebar height:** `.sidebar` must stay `position:sticky; top:0; height:100vh; overflow-y:auto` (desktop) or it stretches to match the tallest page content (CSS Grid default) and the profile card/XP/reset button scroll off-screen on long pages like the exam review screen.
- **`view` must be declared before any code path that can call `render()`/`renderChrome()`** — `touchStreak()` fires at load and cascades into a render; if `var view` isn't initialized before that point (order matters even though `function` declarations hoist, `var` assignments don't), it throws and the whole page renders blank with no visible error unless you check the console.

## Persistence limitation

No cross-device sync capability exists for either published copy. Progress auto-saves to `localStorage` per browser/device (and separately, since the Claude artifact and GitHub Pages copy are different origins — progress does not carry over between them). Export/Import (JSON file) is the manual workaround for moving progress between devices — mention this if it comes up again, it's a platform limitation, not a bug to fix.
